reference/
/target
# /docs is local scratch (protocol reverse-engineering notes and the like) and stays out of the
# repository - but the screenshots the README points at have to be in it, or they render as
# broken images on GitHub.
/docs/*
!/docs/screenshots/
/.claude
/.agents
skills-lock.json
.DS_Store
